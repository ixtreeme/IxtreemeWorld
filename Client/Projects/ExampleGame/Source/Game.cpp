// ExampleGame — placeholder game module source (Phase 1: scaffold only, NOT built).
// Future: compiled by the Editor Build pipeline (Editor/Build) into a game-module DLL
// against sdk/include + libs/script/IxModuleApi.h. Engine must never include this.

#include "../../sdk/include/IxModule.h"

// IXMODULE_ENTRY_POINT will be declared here in Phase 2+ (Auriga is the first real game).
